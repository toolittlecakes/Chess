#include "../Figures/Pawn.h"
#include "../Figures/Board.h"


bool Pawn::ValidOrNot(const std::pair<int, int>& current, const std::pair<int, int>& coord, Board& b)
{
    int color 


    if(!b.CheckBusyOrNot({current.first+1, current.second))
    {
        return true;
    }

    if(b.CheckBusyOrNot({current.first + 1, current.second+1} ==  ))

    return true;
}